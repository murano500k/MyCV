<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TextAppearance.CardHeader" parent="TextAppearance.AppCompat.SearchResult.Title">
        <item name="android:padding">@dimen/activity_vertical_margin</item>
        <item name="android:background">@color/colorAccent</item>
        <item name="android:textColor">@color/textColorHeader</item>
    </style>

    <style name="TextAppearance.Period" parent="TextAppearance.AppCompat.Medium">
        <item name="android:layout_margin">@dimen/content_text_margin</item>
        <item name="android:textColor">@color/textColorPeriod</item>
        <item name="android:textStyle">italic</item>
    </style>

    <style name="TextAppearance.Header" parent="TextAppearance.AppCompat.Large">
        <item name="android:textColor">@color/textPrimary</item>
    </style>
    <style name="TextAppearance.Content" parent="TextAppearance.AppCompat.Small">
        <item name="android:layout_margin">@dimen/content_text_margin</item>
        <item name="android:textColor">@color/textColorDescription</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

</resources>
